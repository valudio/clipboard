{
  "name": "@valudio/selected-text",
  "version": "1.0.0",
  "description": "Get the user's selected text in any active window. Windows only.",
  "main": "index.js",
  "gypfile": true,
  "scripts": {
    "build": "npm run clean && npm run gyp",
    "gyp": "node-gyp configure build",
    "clean": "rimraf build",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "selectedtext",
    "text",
    "selection",
    "windows"
  ],
  "author": " Valudio <development@valudio.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/valudio/selected-text"
  },
  
  "dependencies": {
    "bindings": "^1.2.1",
    "nan": "^2.6.2"
  },
  "devDependencies": {
    "node-gyp": "^3.6.2",
    "rimraf": "^2.6.1"
  }
}
